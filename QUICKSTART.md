# 🚀 Быстрый старт - DNS Tunnel Pro

## 📦 Установка сервера (5 минут)

### 1️⃣ Подготовка

Вам понадобится:
- VPS с Ubuntu 20.04+ (минимум 512MB RAM)
- Публичный IP адрес
- Свой домен (например: `tunnel.example.com`)

### 2️⃣ Установка одной командой

```bash
curl -sSL https://raw.githubusercontent.com/Mr-X-01/dns-tunnel-pro/main/install.sh | sudo bash
```

Или клонируйте репозиторий:

```bash
git clone https://github.com/Mr-X-01/dns-tunnel-pro.git
cd dns-tunnel-pro
sudo ./install.sh
```

**Во время установки вас спросят:**

1. **Домен** (например: `tunnel.example.com`)
   - Валидация формата домена
   - Проверка доступности

2. **Email для Let's Encrypt**
   - Используется для генерации SSL сертификатов
   - Получение уведомлений о продлении

3. **Порт веб-панели** (по умолчанию: случайный 10000-60000)
   - Можете указать свой порт
   - Автоматическая проверка доступности

4. **Пароль администратора**
   - Минимум 8 символов
   - Требуется подтверждение

**Скрипт автоматически:**
- ✅ Установит все зависимости
- ✅ Настроит UFW firewall
- ✅ Сгенерирует Let's Encrypt SSL сертификаты
- ✅ Откроет необходимые порты
- ✅ Создаст systemd сервис
- ✅ Настроит автообновление сертификатов

### 3️⃣ Настройте DNS записи

В панели управления вашего домена добавьте:

```
tunnel.example.com.  IN  NS  ns.example.com.
ns.example.com.      IN  A   YOUR_SERVER_IP
```

Замените:
- `tunnel.example.com` на ваш домен
- `YOUR_SERVER_IP` на IP вашего VPS

### 4️⃣ Запустите сервер

```bash
systemctl start dns-tunnel
systemctl enable dns-tunnel
```

Проверьте статус:
```bash
systemctl status dns-tunnel
```

### 5️⃣ Откройте веб-панель

**Веб-панель доступна по адресу:**
```
https://tunnel.example.com:CUSTOM_PORT
```

> Замените `CUSTOM_PORT` на порт, который вы указали при установке

**Данные для входа:**
- 👤 Логин: `admin`
- 🔑 Пароль: тот что вы указали при установке

**SSL Сертификат:**
- ✅ Let's Encrypt (если домен настроен правильно)
- ⚠️ Self-signed (временный, если домен не доступен)

> 🔐 **ВАЖНО**: Сразу смените пароль в настройках после первого входа!

### 6️⃣ Проверка установки

```bash
# Статус сервиса
systemctl status dns-tunnel

# Логи
journalctl -u dns-tunnel -f

# Порты firewall
sudo ufw status

# Информация об установке
cat /opt/dns-tunnel-pro/INSTALL_INFO.txt
```

---

## 💻 Установка клиента (2 минуты)

### 1️⃣ Установка на Linux

```bash
curl -sSL https://raw.githubusercontent.com/Mr-X-01/dns-tunnel-pro/main/client-install.sh | bash
```

### 2️⃣ Создайте клиента в веб-панели

1. Откройте веб-панель
2. Перейдите в раздел "Клиенты"
3. Нажмите "+ Добавить клиента"
4. Введите имя (например: "Мой ноутбук")
5. Нажмите "Создать"
6. Скачайте конфигурационный файл

### 3️⃣ Подключитесь

Скопируйте конфиг на клиентский компьютер и запустите:

```bash
dns-tunnel-client connect config.json
```

Вы увидите:
```
═══════════════════════════════════════════════════
DNS Tunnel Pro Client Starting...
═══════════════════════════════════════════════════
DNS Domain: tunnel.example.com
DoH Resolver: https://common.dot.dns.yandex.net/dns-query
SOCKS5 Port: 1080
═══════════════════════════════════════════════════
✓ SOCKS5 server started on 127.0.0.1:1080
✓ Client is ready!
✓ Use SOCKS5 proxy: 127.0.0.1:1080
═══════════════════════════════════════════════════
```

### 4️⃣ Проверьте подключение

Откройте новый терминал и проверьте:

```bash
curl --socks5 127.0.0.1:1080 https://ipinfo.io
```

Вы должны увидеть IP адрес вашего VPS сервера!

---

## 🎯 Использование

### В браузере

**Firefox:**
1. Настройки → Основные → Параметры сети
2. Ручная настройка прокси
3. SOCKS Host: `127.0.0.1`, Порт: `1080`
4. Выберите SOCKS v5

**Chrome/Chromium:**
```bash
google-chrome --proxy-server="socks5://127.0.0.1:1080"
```

### С curl

```bash
curl --socks5 127.0.0.1:1080 https://example.com
```

### С wget

```bash
wget -e use_proxy=yes -e socks_proxy=127.0.0.1:1080 https://example.com
```

### С git

```bash
git config --global http.proxy socks5://127.0.0.1:1080
```

---

## 🐳 Быстрый старт с Docker

```bash
git clone https://github.com/Mr-X-01/dns-tunnel-pro.git
cd dns-tunnel-pro
docker-compose up -d
```

Откройте: `https://localhost:8443`

---

## 🔧 Управление

### Сервер

**Запуск:**
```bash
systemctl start dns-tunnel
```

**Остановка:**
```bash
systemctl stop dns-tunnel
```

**Перезапуск:**
```bash
systemctl restart dns-tunnel
```

**Логи:**
```bash
journalctl -u dns-tunnel -f
```

Или:
```bash
tail -f /opt/dns-tunnel-pro/server/logs/server.log
```

### Клиент

**Запуск:**
```bash
dns-tunnel-client connect config.json
```

**Остановка:**
Нажмите `Ctrl+C`

**Тест конфига:**
```bash
dns-tunnel-client test config.json
```

---

## ❓ Частые вопросы

**Q: Как изменить порт SOCKS5?**

A: Отредактируйте конфиг клиента:
```json
{
  "socks5_port": 8080
}
```

**Q: Можно ли использовать несколько клиентов?**

A: Да! Создайте несколько клиентов в веб-панели, каждый получит свой конфиг.

**Q: Как проверить, что туннель работает?**

A: Используйте:
```bash
curl --socks5 127.0.0.1:1080 https://ipinfo.io
```

Должен вернуться IP вашего сервера.

**Q: Клиент не подключается, что делать?**

A: Проверьте:
1. DNS записи настроены правильно
2. Сервер запущен: `systemctl status dns-tunnel`
3. Порт 53 открыт на сервере
4. DoH резолвер доступен

---

## 🆘 Помощь

**GitHub Issues:** https://github.com/Mr-X-01/dns-tunnel-pro/issues

**Telegram:** @Mr_X_01 (если есть)

---

**Готово! Наслаждайтесь свободным интернетом! 🚀**
