# 🏗️ Архитектура DNS Tunnel Pro

## Общая схема

```
┌─────────────────┐      DNS Queries (DoH)     ┌──────────────────┐
│  Linux Client   │◄─────────────────────────►│  Yandex DNS      │
│  (SOCKS5 Proxy) │                            │  (DoH Resolver)  │
└─────────────────┘                            └──────────────────┘
        │                                               │
        │                                               ▼
        │                                      ┌──────────────────┐
        │                                      │   Your Domain    │
        │                                      │   DNS Server     │
        │                                      └──────────────────┘
        │                                               │
        └───────────────────────────────────────────────┘
                                                        │
                                                        ▼
                                               ┌──────────────────┐
                                               │   Web Panel      │
                                               │   (Port 8443)    │
                                               └──────────────────┘
```

## Компоненты системы

### 1. Серверная часть

#### DNS Server (`server/dns_server/server.py`)
- Слушает UDP порт 53
- Обрабатывает DNS запросы
- Извлекает туннелированные данные из DNS запросов
- Проксирует запросы через DoH (Яндекс DNS)
- Возвращает ответы через DNS TXT записи

**Технологии:**
- `dnslib` - парсинг и создание DNS пакетов
- `requests` - DoH запросы
- `cryptography` - AES-256-GCM шифрование

#### Web Panel (`server/web_panel/app.py`)
- Flask приложение
- HTTPS на порту 8443
- SQLite база данных
- Управление клиентами
- Генерация конфигов
- Мониторинг статистики

**Технологии:**
- `Flask` - веб-фреймворк
- `Flask-Login` - аутентификация
- `Flask-SQLAlchemy` - ORM
- Bootstrap CSS (встроенный)

### 2. Клиентская часть

#### DNS Client (`client/dns_client.py`)
- SOCKS5 прокси сервер (порт 1080)
- Шифрование данных AES-256-GCM
- Кодирование в DNS subdomain
- DoH запросы к Яндекс DNS
- Декодирование ответов

**Технологии:**
- `socket` - SOCKS5 сервер
- `cryptography` - шифрование
- `requests` - DoH клиент

## Поток данных

### Запрос (Client → Server)

1. **Приложение** делает запрос через SOCKS5 (127.0.0.1:1080)
2. **DNS Client** принимает SOCKS5 соединение
3. **Клиент** формирует HTTP запрос:
   ```json
   {
     "url": "https://example.com",
     "method": "GET",
     "headers": {...},
     "body": "..."
   }
   ```
4. **Шифрование** AES-256-GCM с уникальным nonce
5. **Кодирование** в base64 (DNS-safe)
6. **Формирование DNS запроса**:
   ```
   base64data.base64data.tunnel.example.com
   ```
7. **DoH запрос** к Яндекс DNS через HTTPS
8. **Яндекс DNS** резолвит домен → перенаправляет на ваш DNS сервер
9. **DNS Server** получает запрос
10. **Декодирование** subdomain → извлечение данных
11. **Расшифровка** AES-256-GCM
12. **Выполнение** реального HTTP запроса
13. **Получение ответа** от целевого сервера

### Ответ (Server → Client)

1. **Server** получает HTTP ответ
2. **Шифрование** ответа AES-256-GCM
3. **Кодирование** в base64
4. **Формирование DNS ответа** (TXT запись):
   ```
   TXT "base64_encrypted_response"
   ```
5. **Отправка** DNS ответа
6. **Яндекс DNS** пересылает ответ клиенту
7. **Client** получает DNS ответ через DoH
8. **Извлечение** данных из TXT записи
9. **Декодирование** base64
10. **Расшифровка** AES-256-GCM
11. **Парсинг** HTTP ответа
12. **Отправка** через SOCKS5 приложению

## Безопасность

### Шифрование

```python
# Генерация ключа (при создании клиента)
key = AESGCM.generate_key(bit_length=256)

# Шифрование
nonce = os.urandom(12)  # 96-bit nonce
ciphertext = aesgcm.encrypt(nonce, plaintext, None)
encrypted = nonce + ciphertext

# Расшифровка
nonce = encrypted[:12]
ciphertext = encrypted[12:]
plaintext = aesgcm.decrypt(nonce, ciphertext, None)
```

### Аутентификация

- Каждый клиент имеет уникальный `client_id`
- Только клиенты с зарегистрированным ID обслуживаются
- Пароли хешируются с `werkzeug.security`

### Transport Security

- Веб-панель работает по HTTPS (TLS)
- DoH использует HTTPS (шифрованный DNS)
- Данные в DNS дополнительно шифруются AES-256

## База данных

### Таблица Users

```sql
CREATE TABLE user (
    id INTEGER PRIMARY KEY,
    username VARCHAR(80) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    is_admin BOOLEAN DEFAULT 1,
    created_at DATETIME
);
```

### Таблица Clients

```sql
CREATE TABLE client (
    id INTEGER PRIMARY KEY,
    client_id VARCHAR(64) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,
    encryption_key VARCHAR(255) NOT NULL,
    created_at DATETIME,
    last_seen DATETIME,
    is_active BOOLEAN DEFAULT 1,
    bytes_sent BIGINT DEFAULT 0,
    bytes_received BIGINT DEFAULT 0,
    notes TEXT
);
```

## Производительность

### Ограничения

- **DNS пакет**: максимум 512 байт (UDP)
- **TXT запись**: максимум 255 символов
- **DoH latency**: ~100-300ms
- **Throughput**: ~1.5 Мбит/с (теоретически)

### Оптимизации

- Асинхронная обработка запросов (threading)
- Кеширование DNS ответов
- Compression данных (опционально)
- Connection pooling для HTTP запросов

## Масштабируемость

### Вертикальная

- Увеличение RAM для больше клиентов
- Faster CPU для шифрования
- SSD для логов и БД

### Горизонтальная

- Несколько DNS серверов (Round-robin DNS)
- Load balancer перед веб-панелью
- Distributed database (PostgreSQL вместо SQLite)

## Мониторинг

### Метрики

- Количество активных клиентов
- Объем трафика (bytes sent/received)
- Ошибки DNS резолвинга
- Задержка ответов
- CPU/Memory usage

### Логи

```
/opt/dns-tunnel-pro/server/logs/server.log
```

Формат:
```
2025-01-29 10:15:30 - INFO - Client registered: abc123...
2025-01-29 10:15:45 - INFO - DNS Query: xyz.tunnel.example.com (TXT)
2025-01-29 10:15:46 - INFO - Request processed: 1.2s
```

## Отказоустойчивость

### Auto-reconnect

Клиент автоматически переподключается при обрыве

### Failover

Можно настроить несколько DoH резолверов:
- Яндекс: `https://common.dot.dns.yandex.net/dns-query`
- Google: `https://dns.google/dns-query`
- Cloudflare: `https://cloudflare-dns.com/dns-query`

### Health checks

Веб-панель предоставляет `/api/stats` для мониторинга

## Развертывание

### Без Docker

```bash
./install.sh  # Автоматическая установка
systemctl start dns-tunnel
```

### С Docker

```bash
docker-compose up -d
```

### Kubernetes (будущее)

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dns-tunnel-server
spec:
  replicas: 3
  ...
```

---

**Архитектура спроектирована для максимальной стабильности и безопасности! 🚀**
